To activate the Python environment :
- Set the working directory to the root of the project
- Execute the command : source .venv/bin/activate

From now on, use pip install ... to install packages.
Use pip freeze > requirements.txt to update the list of installed packages, and pip install -r requirements.txt to install them.